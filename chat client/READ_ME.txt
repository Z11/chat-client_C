/* 
 * NOTE: client and server chat program. This program is created in C using sockets 
 *       (SOCKET_STREAM) to make the necessary connections. As specified in the assignment  
 *       sheet, this is a one client and one server chat program. In other words, this  
 *	 does not implement multiple clients for one server.  
 *
 *        (You must set up the server side first)
 *       TO RUN FROM TERMINAL USE THESE COMMANDS:
 *
 *           gcc server.c -o server
 *           ./server
 *
 *
 *            gcc client.c -o client
 *           ./client
 */